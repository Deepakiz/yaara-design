services:
  - type: web
    name: yaara-design
    env: python
    buildCommand: pip install -r requirements.txt && pip install flask gunicorn
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
